// test function calls
int fib(int n) {
    if(n <= 1)
        return 1;
    else
        return fib(n - 1) + fib(n - 2);
}

int main() {
    int a;
    a = read();
    write(fib(a));
    return 0;
}
